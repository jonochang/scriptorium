# Android app scaffold

Planned integration: consume `bookstore-mobile` through JNI.

Suggested next steps:
1. Add cargo-ndk build pipeline.
2. Generate Kotlin bindings (JNI/UniFFI).
3. Connect Jetpack Compose screens to Rust APIs.

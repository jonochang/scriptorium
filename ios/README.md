# iOS app scaffold

Planned integration: consume `bookstore-mobile` as a static library or XCFramework.

Suggested next steps:
1. Add UniFFI or cbindgen bindings.
2. Create Swift package wrapper.
3. Wire native UI to Rust catalog/inventory APIs.
